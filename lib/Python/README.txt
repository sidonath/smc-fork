


                              SMC -
                     The State Machine Compiler


+ Building & Installing the Python library
------------------------------------------

1. If you have built the Python statemap module previously and
   want to perform a completely new build, then do

   $ make clean

2. To install the Python statemap module on the computer, do:

   $ make install

3. To generate the Python documentation statemap.html, do:

   $ make docs
