


                              SMC -
                     The State Machine Compiler


+ Installing statemap.tcl
-------------------------

NOTE: There is nothing to build - only installation. The Tcl
      package is installed in lib/statemap1.0 so it can be
      found by the "package require" command.

$ make install
